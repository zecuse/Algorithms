#include <iostream>
#include <stop_token>
#include <thread>

using namespace std;

int main()
{
	//stop_source src;
	//jthread ui(UserInput, src, ref(data));
	//jthread tick(DoTicking, src.get_token(), ref(data));
	return 0;
}
